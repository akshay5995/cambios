import { packageName, since } from "./inputs";
import {
  setEnvChangeDetected,
  outputChangedPackages,
  setFailedMessage,
} from "./outputs";

export {
  packageName,
  since,
  setEnvChangeDetected,
  outputChangedPackages,
  setFailedMessage,
};
